{
  "name": "exercise-3-model-bakeoff",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Exercise 3 — Choosing the Right Model (Playwright test-automation bake-off)",
  "scripts": {
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium",
    "serve": "node server.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0"
  }
}
